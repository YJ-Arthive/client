import BoardHeader from '../../components/BoardHeader';

const Gallery = () => {
  return (
    <div>
      <BoardHeader text='Gallery' />
      Gallery 페이지
    </div>
  );
};

export default Gallery;
