import BoardHeader from '../../components/BoardHeader';

const ArtistDetail = () => {
  return (
    <div>
      <BoardHeader text='Artist' />
      ArtistDetail 페이지
    </div>
  );
};

export default ArtistDetail;
